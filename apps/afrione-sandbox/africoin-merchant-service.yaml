name: africoin-merchant-service
project: afrione-api-project
namespace: sandbox
values: |-
  fullnameOverride: africoin-merchant-service
  image:
    repository: afrioneltd/africoin-merchant-service:app-77
  namespace: sandbox
  containerPort: 9101
  autoscaling:
    enabled: false
  role:
    enabled: true
  ingress:
    host: api-merchant-staging
    domain: afrione.co
  resources:
    requests:
      memory: "500Mi"
      cpu: "150m"
    limits:
      memory: "1000Mi"
      cpu: "800m"
  lProbe:
    enabled: true
    path: /actuator/health
    failureThreshold: 5
    periodSeconds: 15
    initialDelaySeconds: 120
  rProbe:
    enabled: true
    path: /actuator/health
    failureThreshold: 5
    periodSeconds: 15
    initialDelaySeconds: 120
  env:
    - name: SPRING_PROFILES_ACTIVE
      value: sandbox
    - name: DATABASE_USERNAME
      valueFrom:
        configMapKeyRef:
          name: configs
          key: DATABASE_USERNAME
    - name: DATABASE_PASSWORD
      valueFrom:
        configMapKeyRef:
          name: configs
          key: DATABASE_PASSWORD
    - name: DATABASE_URL
      valueFrom:
        configMapKeyRef:
          name: configs
          key: DATABASE_URL
  volumeMounts:
    - name: config
      mountPath: "/africoin-merchant/config"
      readOnly: true
  volumes:
    - name: config
      configMap:
        name: africoin-merchant-service
